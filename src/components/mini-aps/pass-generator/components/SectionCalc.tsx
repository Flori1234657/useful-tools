import { TopSection, MiddleSection, DownSection } from "./calcSection";
import { Stack } from "@mui/joy";

const SectionCalc = () => {
  return (
    <Stack>
      <TopSection />
      <MiddleSection />
      <DownSection />
    </Stack>
  );
};

export default SectionCalc;
